## 1. Tests

- [x] 1.1 Add `ProjectManagerPanel` tests proving recent and management variants use the compact-grid layout contract.
- [x] 1.2 Add a `ProjectManagerPanel` test proving recent project cards expose compact grid card metadata while preserving the direct continue action.

## 2. Implementation

- [x] 2.1 Update `ProjectManagerPanel` to render both recent and management project lists with the compact responsive grid layout.
- [x] 2.2 Update `ProjectCard` recent-card markup/classes so the continue action fits inside compact grid cards.
- [x] 2.3 Update launcher CSS for compact project grid spacing, card density, and selected/hover states.

## 3. Verification

- [x] 3.1 Run targeted startup project manager tests.
- [x] 3.2 Run the web test suite.
- [x] 3.3 Run the web build.
- [x] 3.4 Validate the OpenSpec change if the CLI is available.
